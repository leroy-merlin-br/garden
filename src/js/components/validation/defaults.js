import required from 'scripts/components/validation/required'
import confirm from 'scripts/components/validation/confirm'
import minlength from 'scripts/components/validation/minlength'
import maxlength from 'scripts/components/validation/maxlength'
import email from 'scripts/components/validation/email'

export default {
  required: required,
  confirm: confirm,
  minlength: minlength,
  maxlength: maxlength,
  email: email
}
